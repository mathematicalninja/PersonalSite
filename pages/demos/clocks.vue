<template></template>

<script lang="ts" setup>
    // piTime = 3:08:29
    // eTime = 2:43:05

    // Idea 1: if the time is piTime or eTime, then the clock will show e.g. "e O'clock", otherwise it will show the time.
    // Idea 2: the clock will get the system date format and map YYYY ==> HH, MM ==> MM, DD ==> SS, and then show the time in that format.
    // Idea 3: combining these two, times will be shown based on the sys date format.
    // Idea 4: add graphical clocks.
</script>

<style></style>
